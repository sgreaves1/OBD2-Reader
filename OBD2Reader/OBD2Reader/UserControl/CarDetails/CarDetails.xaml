<UserControl x:Class="OBD2Reader.UserControl.CarDetails.CarDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="Root">
    <Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Label Content="{Binding ElementName=Root, Path=Car.Make}"/>
            <Label Content="{Binding ElementName=Root, Path=Car.Model}"/>
            <Label Content="{Binding ElementName=Root, Path=Car.Year}"/>
        </StackPanel>
    </Grid>
</UserControl>
